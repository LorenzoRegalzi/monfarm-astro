---
interface Props {
  src: string;
  alt: string;
  height?: string;
  portrait?: boolean;
  solid?: boolean;
  loading?: 'lazy' | 'eager';
}

const {
  src,
  alt,
  height,
  portrait = false,
  solid = false,
  loading = 'lazy',
} = Astro.props;
const resolvedHeight = portrait ? undefined : (height ?? 'var(--image-wide-height)');
---

<div
  class:list={[
    'full-image',
    {
      'full-image--portrait': portrait,
      'full-image--solid': solid,
    },
  ]}
  style={!portrait && resolvedHeight ? `height: ${resolvedHeight};` : undefined}
>
  <img
    src={src}
    alt={alt}
    loading={loading}
    decoding="async"
  />
</div>
