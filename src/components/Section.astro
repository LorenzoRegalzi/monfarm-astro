---
interface Props {
  bg?: 'olive';
}

const { bg } = Astro.props;
---

<section class:list={['section', { 'section--olive': bg === 'olive' }]}>
  <div class="container stack stack--md section__inner">
    <slot />
  </div>
</section>
