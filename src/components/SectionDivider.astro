---
interface Props {
  bar?: boolean;
}

const { bar = true } = Astro.props;
---

<div class="section-divider">
  <div class="section-divider__band"></div>
  {bar && (
    <div class="container">
      <div class="section-divider__bar"></div>
    </div>
  )}
</div>
